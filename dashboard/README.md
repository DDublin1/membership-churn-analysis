# Membership Churn Analysis Dashboard

This is a self-contained interactive HTML dashboard for the membership churn analysis project. Open `index.html` in Chrome to view it — no installation, build step, or internet connection is required. All data is embedded directly from the Gold layer Parquet exports produced in the churn analysis pipeline, converted to JSON and loaded via a local `data.js` file alongside the HTML.
